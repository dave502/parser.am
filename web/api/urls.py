from django.urls import path
#from .views import regions_state

app_name = "api"

urlpatterns:list = [
    #path('process-images/', process_images, name="process-images"),
    #path('process-text/', process_text, name="process-text"),
]